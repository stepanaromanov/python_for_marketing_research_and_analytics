# ============================================================
# Marketing Analytics Portfolio — Full Stack Requirements
# Python 3.12 | Last updated: 2025
# Install: pip install -r requirements.txt
# ============================================================

# ── Core data stack ─────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.2,<3.0
scipy>=1.13

# ── Visualisation ───────────────────────────────────────────
plotly==5.24.1
matplotlib>=3.9
seaborn>=0.13
kaleido==0.1.0         # plotly static image export (PNG/SVG)

# ── Attribution & graph (Projects 01, 05, 12) ───────────────
networkx>=3.3

# ── Shapley & explainability (Projects 02, 04, 05, 15) ──────
shap>=0.46

# ── Bayesian / MMM (Projects 03, 06, 07, 08, 09, 10) ────────
# pymc>=5.16              # uncomment — large install, needs C compiler
# meridian-mmm>=0.1       # uncomment — Google's MMM library (pip install meridian)

# ── Causal ML / uplift (Projects 11, 13) ────────────────────
econml>=0.15
scikit-learn>=1.5

# ── Optimisation (Projects 04, 08, 14) ──────────────────────
PuLP>=2.9

# ── Statistics & experimentation (Project 17) ───────────────
statsmodels>=0.14
pingouin>=0.5.4

# ── MLflow — experiment tracking & model registry ───────────
mlflow>=2.14
# mlflow[extras]          # uncomment for ONNX + Triton support

# ── DVC — pipeline versioning & feature store ───────────────
dvc>=3.51
# dvc[s3]                 # uncomment if using S3 remote storage
# dvc[gdrive]             # uncomment if using Google Drive remote

# ── Drift monitoring (all MLOps projects) ───────────────────
evidently>=0.4.33

# ── Workflow orchestration ───────────────────────────────────
# apache-airflow>=2.9     # uncomment — very large; use Docker instead
# See: https://airflow.apache.org/docs/apache-airflow/stable/start.html

# ── API serving (Project 01 FastAPI endpoint) ───────────────
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7

# ── Streaming MLOps (Project 12) ────────────────────────────
# kafka-python>=2.0       # uncomment when needed

# ── Notebook environment ─────────────────────────────────────
jupyterlab>=4.2
ipywidgets>=8.1
nbformat>=5.10
nbconvert>=7.16

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=1.0
pyyaml>=6.0
tqdm>=4.66
joblib>=1.4
